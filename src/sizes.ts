const sectionSize = 30;
const borderSize = 10;
const counterHeight = 30;
const counterWidth = 17;
const headerSize = borderSize * 2 + counterHeight;
const resetButtonSize = counterHeight;

export {
  sectionSize,
  borderSize,
  headerSize,
  counterHeight,
  counterWidth,
  resetButtonSize,
};
